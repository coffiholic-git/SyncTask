# Frontend

This dependency-free frontend is served as static files. Start it from this directory with:

```powershell
python -m http.server 5500
```

Then open `http://localhost:5500`. Its API base URL is set to `http://localhost:8081` in `app.js`.
